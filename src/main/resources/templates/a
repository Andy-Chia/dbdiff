帮我用java程序实现一下功能，
我现在有两个数据库，一个是标准数据库，另外一个是旧版本的数据库，为了对比他们的差异，并生成差异升级脚本，我定义了几个类对象，并且已经从数据库读取到了对应的值，但是我需要你帮我完成这几个对象，来对比和生成差异的代码，需要写好注释，并且可以执行。
类对象如下：
表类
public class Table {
    private  String name;
    private  List<Column> columns;
    private  Index primaryKey;
    private  List<Index> indexes;
    private  String comment;
}
// 索引类
public class Index {
    private String name;
    private String type;
    private List<String> columns;
    private String table;
    private Boolean nonUnique;
}
列类
public class Column {
    private String name;
    private String type;
    private String length = "";
    private Boolean isNullable;
    private String defaultValue;
    private String comment;

    private String table;
}